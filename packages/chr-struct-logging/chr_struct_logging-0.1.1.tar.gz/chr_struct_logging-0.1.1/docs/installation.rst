.. highlight:: shell

============
Installation
============


Stable release
--------------

To install CHR structured logging, run this command in your terminal:

.. code-block:: console

    $ pip install chr_struct_logging

This is the preferred method to install CHR structured logging, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for CHR structured logging can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/yordan-bautista-chr/chr_struct_logging

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/yordan-bautista-chr/chr_struct_logging/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/yordan-bautista-chr/chr_struct_logging
.. _tarball: https://github.com/yordan-bautista-chr/chr_struct_logging/tarball/master
